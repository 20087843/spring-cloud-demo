spring:
  application:
    name: order-service

  cloud:
    config:
      enabled: true
      uri: http://localhost:8888/
      profile: dev
      label: master
      discovery:
        enable: true
        service-id: config-server

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

